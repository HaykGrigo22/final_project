// scripts.js

document.querySelectorAll('.btn-add-to-cart').forEach(button => {
    button.addEventListener('click', () => {
        alert('Product added to cart!'); // Добавьте реальную логику здесь
    });
});
